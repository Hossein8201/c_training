#include <stdio.h>
#include <string.h>

int main(){
    int m,n,street[1000000],in=0,out=0;
    // char cyrcle[1000000];
    scanf("%d %d",&n,&m);
    for (int i=0;i<n;i++){
        scanf("%d ",& street[i]);
        // if (street[i]==1)   out =1;
        // else in=1;
    }
    // scanf("%s",cyrcle);
    // if (in == out && out ==1)  printf("YES");
    // else  printf("NO");
}